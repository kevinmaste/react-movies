import {makeStyles} from "@material-ui/core";

export const useStyles = makeStyles((theme) => ({
    paddingGrid:{
        marginTop:"56px",
    },
    form_control:{
        width:"25%",
    },
    paper_style:{
        textAlign:"center",
    },
    dis_lik:{
        display:"flex",
    }
}))